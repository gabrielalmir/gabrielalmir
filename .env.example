# Public env (optional). Copy to `.env` locally if needed.
# Three.js signature field: default ON. Set to 0/false to force 2D craft only.
# Runtime overrides: ?webgl=0 or ?webgl=1
PUBLIC_THREE_SIGNATURE=1
